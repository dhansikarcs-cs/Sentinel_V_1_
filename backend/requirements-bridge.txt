# Extra dependencies for the ring bridge only (not the core backend).
# The BLE adapter (app/services/ring/ble_gatt.py) imports bleak lazily, so the
# main API runs fine without this file.
bleak>=0.21.0
